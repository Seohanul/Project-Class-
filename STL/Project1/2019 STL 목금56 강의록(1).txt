

=========================================
Fri Mar  8 14:00:54 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------

#include <iostream>
#include <fstream>
#include <string>
#include <algorithm>
#include <iterator>
#include <chrono>


void save(std::string);

int main()
{
	save("소스.cpp");
}

void save(std::string fname)
{
	// fname을 읽기 모드로 연다.
	std::ifstream in(fname);

	if (!in)
		;// 오류 처리


		 // 기록할 파일을 덧붙여쓰기 모드로 연다.
	std::ofstream out("2019 STL 목금56 강의록(1).txt", std::ios::app);




	auto tic = std::chrono::system_clock::now();


	auto time = std::chrono::system_clock::to_time_t(tic);


	std::string date = ctime(&time);

	out << std::endl << std::endl;
	out << "=========================================" << std::endl;
	out << date;
	out << "=========================================" << std::endl;
	out << std::endl;

	// fname의 모든 내용을 덧붙여 쓴다.
	// std::copy(어디부터, 어디까지 , 어디로)

	std::copy(std::istreambuf_iterator<char>(in), std::istreambuf_iterator<char>(), std::ostreambuf_iterator<char>(out));
	// 인자 없는 생성자로 마지막을 지정함

	// 실행된 시간을 기록한다.
}




=========================================
Fri Mar  8 14:04:36 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------

#include <iostream>
#include "save.h"
int main()
{
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:05:29 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------

#include <iostream>
#include "save.h"
int main()
{
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:05:49 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------

#include <iostream>
#include "save.h"
int main()
{
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:09:07 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>

#include "save.h"

int main()
{
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:09:17 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>

#include "save.h"

int main()
{
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:09:24 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>

#include "save.h"

int main()
{
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:34:22 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	std::sort(std::begin(a), std::end(a));
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:41:22 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	sort(begin(a), end(a));

	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:48:17 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	sort(begin(a)+2, end(a)-4);

	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:48:33 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	sort(begin(a) + 2, end(a) - 3);

	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:58:17 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;

bool how(int, int);

bool how(int a, int b)
{
	return a > b;
}

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	
	sort(begin(a), end(a), how);
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:58:25 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;

bool how(int, int);

bool how(int a, int b)
{
	return a < b;
}

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	
	sort(begin(a), end(a), how);
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 14:58:58 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;

bool how(int, int);

bool how(int a, int b)
{
	return a > b;
}

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	
	sort(begin(a), end(a), how);
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 15:01:44 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;

bool how(int, int);

bool how(int a, int b)
{
	static int count = 0;
	count++;
	cout << count << endl;
	return a > b;
}

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	
	sort(begin(a), end(a), how);
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 15:04:52 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;

bool how(int, int);

bool how(int a, int b)
{
	static int count = 0;
	count++;
	cout << count << endl;
	return a > b;
}

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	
	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
	});
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 15:06:19 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;

bool how(int, int);

bool how(int a, int b)
{
	static int count = 0;
	count++;
	cout << count << endl;
	return a > b;
}

int main()
{
	// 정수 10개를 정렬해보자
	int a[10] = {1, 3, 5, 7, 9, 2, 4, 6, 8, 10};
					// 초기화자 목록(Initializer_list)
	
	
	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
	});
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 15:07:02 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;



int main()
{
	// 정수 1000개를 정렬해보자
	int a[1000];

	for (int&d : a)
		d = 1;
	
	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
	});
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 15:07:11 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;



int main()
{
	// 정수 1000개를 정렬해보자
	int a[1000];

	for (int&d : a)
		d = 3;
	
	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
	});
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 15:07:28 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;



int main()
{
	// 정수 1000개를 정렬해보자
	int a[1000];

	for (int&d : a)
		d++;
	
	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
	});
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 15:07:37 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"


using namespace std;



int main()
{
	// 정수 1000개를 정렬해보자
	int a[1000] = { 0, };

	for (int&d : a)
		d++;
	
	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
	});
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 15:12:44 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"

#include <random>
using namespace std;



int main()
{
	// 정수 1000개를 정렬해보자
	int a[1000];

	default_random_engine dre;
	uniform_int_distribution<> uid;


	for (int i = 0; i < 1000; ++i)
		a[i] = uid(dre);
	//슈도 랜덤 (진짜 렌덤은 아님)


	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
	});
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;
	save("소스.cpp");
}



=========================================
Fri Mar  8 15:13:10 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"

#include <random>
using namespace std;



int main()
{
	// 정수 1000개를 정렬해보자
	int a[1000];

	default_random_engine dre;
	uniform_int_distribution<> uid;


	for (int i = 0; i < 1000; ++i)
		a[i] = uid(dre);
	//슈도 랜덤 (진짜 렌덤은 아님)


	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
	});
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;


	save("소스.cpp");
}



=========================================
Fri Mar  8 15:13:33 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"

#include <random>
using namespace std;



int main()
{
	// 정수 1000개를 정렬해보자
	int a[1000];

	default_random_engine dre;
	uniform_int_distribution<> uid(0,1000);


	for (int i = 0; i < 1000; ++i)
		a[i] = uid(dre);
	//슈도 랜덤 (진짜 렌덤은 아님)


	sort(begin(a), end(a), [](int a, int b) {
		return a > b;
	});
	// end는 9번이 아니라 한칸뒤인 10번을 가리킴
	for (int d : a)
		cout << d << ' ';
	cout << endl;


	save("소스.cpp");
}



=========================================
Fri Mar  8 15:14:49 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"

#include <random>
using namespace std;



int main()
{
	// 정수 1000개를 정렬해보자
	int a[1000];

	default_random_engine dre;
	uniform_int_distribution<> uid(0,1000);


	for (int i = 0; i < 1000; ++i)
		a[i] = uid(dre);
	//슈도 랜덤 (진짜 렌덤은 아님)


	sort(begin(a), end(a));

	for (int d : a)
		cout << d << ' ';
	cout << endl;


	save("소스.cpp");
}



=========================================
Fri Mar  8 15:15:49 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"

#include <random>
using namespace std;



int main()
{
	// 정수 1000개를 정렬해보자
	int a[1000];

	default_random_engine dre;
	uniform_int_distribution<> uid(0,1000);


	for (int& d : a)
		d = uid(dre);
	//슈도 랜덤 (진짜 렌덤은 아님)


	sort(begin(a), end(a));

	for (int d : a)
		cout << d << ' ';
	cout << endl;


	save("소스.cpp");
}



=========================================
Fri Mar  8 15:19:31 2019
=========================================

//------------------------------------------------------------------------------------------------
// 2019년 3월 8일 (금) 
//
// 책은 다음주 부터
//------------------------------------------------------------------------------------------------
#include <iostream>
#include <algorithm>

#include "save.h"

#include <random>
using namespace std;

int a[1000000];


int main()
{
	// 정수 1000000개를 정렬해보자

	default_random_engine dre;
	uniform_int_distribution<> uid(0,1000);


	for (int& d : a)
		d = uid(dre);
	//슈도 랜덤 (진짜 렌덤은 아님)


	sort(begin(a), end(a));

	for (int d : a)
		cout << d << ' ';
	cout << endl;


	save("소스.cpp");
}

